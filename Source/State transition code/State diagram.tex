\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows,shapes,automata,petri,positioning,calc}
\documentclass{article}

\tikzset{
    place/.style={
        circle,
        thick,
        draw=black,
        fill=gray!50,
        minimum size=6mm,
    },
        state/.style={
        circle,
        thick,
        draw=black,
        fill=gray!50,
        minimum size=6mm,
    },
}

\begin{document}

\begin{tikzpicture}[node distance=5cm and 4cm,>=stealth',auto, every place/.style={draw}]
    \node [place] (Q=0) {00};
   \node[state] at (4.4, -5.37) (q3) {11};
    \coordinate[node distance=4cm,left of=Q=0] (left-Q=0);
    \coordinate[node distance=4cm,right of=Q=0] (right-Q=0);
     \coordinate[node distance=3cm,below of=Q=0] (below-Q=0);
   

   \node [place] (Q=1) [right=of Q=0] {10};
   \node [place] (Q=2) [below=of Q=0] {01};
   
   
   \path[->] (Q=0) edge [bend left] node {1/0} (q3);
   \path[->] (Q=1) edge [left, above] node {0/0} (Q=0);
   \path[->] (Q=1) edge [loop right] node {1/0} ();
   \path[->] (q3) edge [bend left,above] node {0/1} (Q=0);
    \path[->] (Q=0) edge [below, left] node {0/0} (Q=2);
    \path[->] (Q=2) edge [right, above] node {1/0} (q3);
    \path[->] (q3) edge [above, left] node {1/1} (Q=1); 
     \path[->] (Q=2) edge [loop left] node {0/0} ();
\end{tikzpicture}
\end{document}
